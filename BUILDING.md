## Clone as submodule

```sh
git submodule update --init --recursive --depth 1
```
## Get submodules (SDL3 & cglm)

```sh
git submodule update --init --recursive --depth 1
```
## Configure build folder and build
```sh
cmake -S . -B build
cmake --build build
```
## ~~Run~~

```sh
./build/rmi
```

